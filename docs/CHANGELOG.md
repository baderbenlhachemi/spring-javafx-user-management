# Changelog

This file records repository changes made through `docs/IMPLEMENTATION_PLAN.md`.

Use the task identifier in every entry. Keep entries under **Unreleased** until a release is tagged. Record behavior and verification, not a raw file list.

## Unreleased

### Documentation

- **Task 5.8 / v0.2 Secure Baseline summary:** Reframed the public repository as Team Access Hub and published a concise evaluator path covering the implemented security baseline, current modular-monolith and JavaFX boundaries, prerequisites, externalized configuration, one-command verification, Compose startup and readiness, host development, JavaFX source launch and packaging, the compatible endpoint contract, security/data boundaries, and known limitations. The JavaFX guide no longer advertises a default login and now documents the correct `PUT /api/users/me/password` method, default-denied registration policy, in-memory session behavior, verification, configuration precedence, and platform-native packaging. Project context now describes the verified post-baseline state instead of the original audit findings; architecture distinguishes delivered boundaries from deferred contract work; and the roadmap marks the delivered v0.2 slice while keeping invitations, lifecycle states, revocable sessions, audit history, API versioning, scanning, and portfolio publication explicitly deferred. The documentation directory is no longer ignored, so the published links and checks are present in a clean checkout. All Tasks 1.1 through 5.8 are complete; no architecture, API contract, or database schema changed.
- **Task 5.8 verification:** `SecureBaselineDocumentationTest` passed 5 focused product-name, evaluator-command, endpoint-method, registration-policy, credential-claim, roadmap/status, local-link, and referenced-file checks. The host's policy rejected direct `.\scripts\verify.ps1` execution before the script started, so the documented process-scoped equivalent `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify.ps1` was used and passed with 175 backend tests and 13 JavaFX tests, all with 0 failures, 0 errors, and 0 skipped tests. `docker compose config` passed with disposable validation placeholders. `git diff --check` passed with the required command-scoped safe-directory setting. The Task 5.8 documentation scan was reviewed: public guides contain no runnable default credential or signing assignment; matches outside them are the plan's scan text, historical verification text, or ordinary administrator wording rather than credential claims.
- Added the repository audit, architecture direction, portfolio roadmap, and task-sized secure-baseline implementation plan.

### Security

- **Task 3.2:** Added MVC coverage using the production security filter chain, JWT parser, authentication provider, and disabled-account loader. The tests prove anonymous login, authenticated self-profile access, anonymous and member rejection from an administrator endpoint, administrator access, disabled-user rejection, and invalid/expired JWT rejection. Existing MVC policy tests continue to prove enabled registration compatibility plus disabled registration and Swagger 401/403 behavior. Fixtures use per-test mocks and synthetic users, with no execution-order or pre-existing database dependency. The established global error contract for method-security denial remains unchanged.
- **Task 3.2 verification:** `.\mvnw.cmd "-Dtest=*Security*,*Authorization*" test` passed with 20 tests, 0 failures, and 0 errors. The selector is quoted because PowerShell otherwise parses its comma before Maven receives the argument.
- **Task 2.8:** Replaced exception-derived client messages with stable, non-sensitive messages while preserving the existing `status`, `message`, and `timeStamp` response fields and all existing HTTP status mappings. Validation and malformed JSON now have explicit redacted 400 responses; authentication, authorization, conflict, not-found, I/O, and unexpected failures retain their previous statuses. Internal I/O and unexpected failures log only the exception type without the exception message or stack trace.
- **Task 2.8 verification:** `.\mvnw.cmd -Dtest=*ExceptionHandler* test` passed with 8 tests, 0 failures, and 0 errors. Coverage includes every handled category, exact error-envelope fields, stable status/message behavior, removal of sensitive exception details from responses, and a redacted unexpected-failure log diagnostic.
- **Task 2.7:** Added typed registration and Swagger policy properties and applied them in the existing Spring Security filter chain. Both policies default to disabled in the default profile, the explicit `dev` profile defaults both to enabled for local usability, and `DEMO_REGISTRATION_ENABLED` / `DEMO_SWAGGER_ENABLED` can override them independently. Disabled policies deny anonymous and authenticated access; enabled registration preserves `POST /api/auth/register` and its successful response, while enabled Swagger exposes the existing OpenAPI and Swagger UI route families. The README now documents the actual registration endpoint and both environment policies.
- **Task 2.7 verification:** `.\mvnw.cmd -Dtest=*Security* test` passed with 12 tests, 0 failures, and 0 errors. Coverage includes typed-property binding and safe defaults, committed default/dev profile values, enabled registration compatibility, disabled registration for anonymous and authenticated callers, and enabled/disabled access across both `/v3/api-docs/**` and `/swagger-ui/**` routes.
- **Task 2.6:** Added a transactional last-active-administrator invariant to the existing delete, status, and role-change workflows. Each mutation pessimistically locks the active administrator set before checking and applying the change, so deleting, disabling, or demoting the final active admin returns a stable HTTP 409 domain error while the same operations remain allowed when another active admin exists. Successful endpoint paths, parameters, and response messages remain unchanged; no schema change was required.
- **Task 2.6 verification:** `.\mvnw.cmd -Dtest=*AdminInvariant* test` passed with 11 tests, 0 failures, and 0 errors against PostgreSQL Testcontainers. Coverage includes transactional rejection for delete/disable/demote, all three allowed equivalents with a backup active admin, preserved controller success responses, the stable 409 response, and a simultaneous two-thread disable race that commits one operation, rejects the other, and leaves exactly one active administrator.
- **Task 1.7:** Removed the tracked `users.json` and `new_users.json` runtime exports that contained BCrypt password hashes. Generated user JSON/CSV exports are now ignored throughout the repository.
- **Task 1.7 verification:** `git ls-files` listed no generated export or Maven `target/` content, and `git grep -n '\$2[aby]\$'` returned no matches. Both commands were run with a command-scoped `safe.directory` override because the validation sandbox uses a different Windows account than the repository owner.
- **Task 1.3:** Marked `User.password` as a write-only Jackson property so entity serialization cannot expose the password field or its hash. Deserialization remains enabled for compatibility with the current legacy entity-based JSON input and is covered by a regression test.
- **Task 1.3 verification:** `.\mvnw.cmd -Dtest=UserSerializationTest test` passed with 2 tests, 0 failures, and 0 errors.
- **Task 1.5:** Removed committed PostgreSQL credentials and JWT signing material. Runtime startup now requires `DB_URL`, `DB_USERNAME`, `DB_PASSWORD`, and `JWT_SECRET`, and an early redacted validator reports missing variable names before datasource or JWT initialization. The `test` profile uses isolated Testcontainers connection details and explicitly test-only JWT configuration.
- **Task 1.5 verification:** `.\mvnw.cmd test` passed with 9 tests, 0 failures, and 0 errors. The required credential `rg` scan returned no matches. A credential-free startup probe failed immediately with `DB_URL`, `DB_USERNAME`, `DB_PASSWORD`, and `JWT_SECRET` named as missing.
- **Task 1.6:** Restricted administrator seeding to the explicit `dev` profile while preserving safe role initialization in every profile. Development username, password, and email are externally configured, and initializer logs contain no credential or personal values.
- **Task 1.6 verification:** `.\mvnw.cmd test` passed with 12 tests, 0 failures, and 0 errors. `rg -n 'Password: admin|setPassword\(passwordEncoder\.encode\("admin"\)' src\main` returned no matches.

### Backend

- **Task 4.6:** Replaced the monolithic `UserController` with focused authentication, profile, administrator-user, user-transfer, and user-statistics controllers. All 16 existing operations retain their paths, HTTP methods, method-security expressions, request parameters and bodies, download headers, and successful response shapes; OpenAPI now groups them under five cohesive tags. The legacy controller and its reflection-based test factory were removed, and controller tests now construct the focused boundaries directly. Architecture, service behavior, API contracts, and the database schema are unchanged.
- **Task 4.6 verification:** `.\mvnw.cmd test` passed with 155 tests, 0 failures, and 0 errors. Boundary tests assert the exact controller dependencies, all 16 route mappings, security expressions, and OpenAPI tags; MVC compatibility coverage includes generic username-profile response redaction. After controller deletion, one `.\mvnw.cmd clean` was required to remove Maven's stale compiled `UserController` class before the exact validation command was rerun successfully.

- **Task 4.5:** Extracted fake-user JSON serialization, transactional import delegation, bounded filtered/unfiltered export queries, and CSV production from `UserController` into a cohesive `UserTransferService`. The service composes the existing hardened generation, import, and CSV helpers, preserving generation limits, the 1 MiB and 1–1,000-record import bounds, the 10,000-row export cap, server-owned import credentials, formula neutralization, and in-memory downloads. The controller now only accepts transfer parameters, delegates, maps import counts and stable validation errors, and applies the existing download headers. Endpoint paths, HTTP methods, multipart field, query parameters, filenames, content types, response shapes, architecture, and database schema are unchanged.
- **Task 4.5 verification:** The plan's unquoted `.\mvnw.cmd -Dtest=*Import*,*Export*,*Generation* test` form was rejected by PowerShell's comma parser before Maven started; the equivalent quoted selector `.\mvnw.cmd "-Dtest=*Import*,*Export*,*Generation*" test` passed with 44 tests, 0 failures, and 0 errors, including focused transfer-service tests and PostgreSQL Testcontainers integration coverage. `.\mvnw.cmd test` also passed with 151 tests, 0 failures, and 0 errors. A controller scan found no JSON mapper, CSV helper, filesystem API, export query, persistence call, or per-record loop, and neither `users.json` nor `users_export.csv` existed after validation.
- **Task 4.4:** Extracted user listing, search, bounded pagination, authorized username lookup, ID lookup, administrative profile updates, deletion, role changes, status changes, and user statistics from `UserController` into focused `AdminUserService` and `UserStatisticsService` boundaries. Read workflows are explicitly read-only transactional and administrative mutations are transactional, retaining the existing last-active-administrator protection in the nested user service transaction. The controller now delegates these workflows and maps the established success or validation responses; endpoint paths, methods, parameters, DTO fields, pagination metadata, statistics keys, error messages, architecture, and database schema are unchanged. CSV export querying remains in the controller for the separately scoped Task 4.5.
- **Task 4.4 verification:** The plan's unquoted `.\mvnw.cmd -Dtest=*Admin*,*Stats* test` form was rejected by PowerShell's comma parser before Maven started; the equivalent quoted selector `.\mvnw.cmd "-Dtest=*Admin*,*Stats*" test` passed with 24 tests, 0 failures, and 0 errors, including PostgreSQL Testcontainers last-admin invariant coverage. `.\mvnw.cmd test` also passed with 145 tests, 0 failures, and 0 errors, covering service transactions, search/pagination compatibility, authorized lookup, every administrative mutation, statistics mapping, MVC/security wiring, Flyway, and disposable PostgreSQL integration tests.
- **Task 4.3:** Extracted the three self-service `/api/users/me` workflows from `UserController` into a focused transactional `ProfileService`. The service now resolves the authenticated username, loads the current user, maps redacted profile DTOs, applies compatible partial profile updates, enforces the existing duplicate-email rule, and performs current-password verification plus server-side encoding before persistence. The controller retains only request validation, delegation, and the existing success or duplicate-email HTTP response mapping. Paths, methods, security annotations, request fields, success payloads, error mappings, architecture, and database schema are unchanged.
- **Task 4.3 verification:** `.\mvnw.cmd -Dtest=*Profile* test` passed with 12 tests, 0 failures, and 0 errors. Focused service and controller coverage proves all three endpoint contracts, authenticated current-user lookup, DTO redaction, every mutable profile field, same/duplicate-email behavior, missing-user handling, transactional boundaries, password verification and encoding, successful password responses, and stable invalid-password errors. `.\mvnw.cmd test` also passed with 130 tests, 0 failures, and 0 errors, including security MVC, Spring context, Flyway, and PostgreSQL Testcontainers coverage.
- **Task 4.2:** Extracted login and registration orchestration from `UserController` into a focused transactional `AuthenticationService`. The service now owns user lookup, credential authentication, security-context population, deterministic last-login persistence through the application `Clock`, JWT generation, compatible token-response mapping, duplicate registration checks, password encoding, default-role resolution, and compatible user initialization. The controller retains only request validation and HTTP status/body mapping, including the existing duplicate-registration 400 message payloads. Endpoint paths, methods, request fields, successful response shapes, registration policy, architecture, and database schema are unchanged.
- **Task 4.2 verification:** `.\mvnw.cmd -Dtest=*Auth* test` passed with 13 tests, 0 failures, and 0 errors. New focused service tests prove transactional login and registration methods, exact last-login time, authentication/save/token ordering, compatible JWT fields, unknown-user handling, encoded registration credentials and defaults, and duplicate username/email results. `.\mvnw.cmd test` also passed with 118 tests, 0 failures, and 0 errors, including enabled/disabled registration policy, Spring context, Flyway, and PostgreSQL Testcontainers coverage.
- **Task 4.1:** Replaced backend field injection with explicit constructor injection in the user controller, user and role services, security configuration, JWT filter and utility, user-details service, and data initializer. Required collaborators and injected configuration values are now held in final fields; the security configuration constructs the JWT filter with its dependencies explicitly. Controller tests now use constructor-based setup through a focused test factory instead of mutating private fields, with no lenient or relaxed mocking. Endpoint behavior, configuration keys, API contracts, architecture, and database schema are unchanged.
- **Task 4.1 verification:** `.\mvnw.cmd test` passed with 113 tests, 0 failures, and 0 errors, including Spring context, security MVC, Flyway, and PostgreSQL Testcontainers coverage. `rg -n "@Autowired\s*$" src\main\java` returned no matches, so no remaining production field injection requires justification.
- **Task 3.4:** Added full-stack MockMvc integration coverage for the existing import, fake-generation, and CSV-export endpoints using Flyway and disposable PostgreSQL Testcontainers. JSON fixtures prove valid imports, malformed-input rejection, and privileged-field attempts; database assertions prove imported accounts remain disabled, receive server-owned BCrypt credentials and identifiers, and count both persisted and in-file duplicates deterministically. Additional HTTP coverage rejects unsafe import and generation counts, verifies generated JSON is disabled and credential-free, and exercises CSV commas, quotes, embedded newlines, Unicode, and all four spreadsheet-formula prefixes. Every test asserts that neither `users.json` nor `users_export.csv` is created in the repository root. No production code, API contract, architecture, or schema changed.
- **Task 3.4 verification:** `.\mvnw.cmd "-Dtest=*Import*,*Export*,*Generation*" test` passed with 38 tests, 0 failures, and 0 errors, including 11 new integration cases. The selector is quoted so PowerShell passes its commas to Maven as one argument.
- **Task 3.3:** Added an application `Clock` dependency to `UserService`, backed by the system-default zone in runtime configuration, so the start-of-day cutoff for new-user statistics is deterministic and test-injectable without changing API behavior. Added PostgreSQL Testcontainers service coverage for username and email uniqueness, successful role changes, disable/re-enable persistence, and an exact fixed-clock day boundary. Existing PostgreSQL last-admin tests continue to cover delete, disable, demotion, allowed equivalents, and concurrent mutation protection.
- **Task 3.3 verification:** `.\mvnw.cmd "-Dtest=*UserService*,*AdminInvariant*" test` passed with 16 tests, 0 failures, and 0 errors. The selector is quoted for PowerShell because it contains a comma.
- **Task 2.5:** Moved CSV construction into a focused export service while preserving `GET /api/users/export/csv`, the optional `search` parameter, the `users_export.csv` filename, and the existing 14-column order. Export queries are capped at 10,000 rows, output is encoded explicitly as UTF-8, values beginning with `=`, `+`, `-`, or `@` are prefixed with an apostrophe before structural CSV escaping, and comma, quote, LF, and CR characters remain correctly escaped. The root README documents the export cap and spreadsheet safety behavior.
- **Task 2.5 verification:** `.\mvnw.cmd -Dtest=*Csv* test` passed with 9 tests, 0 failures, and 0 errors. `rg -n "Integer\.MAX_VALUE" src\main\java` returned no matches. Coverage includes all four formula prefixes, UTF-8 bytes, comma/quote/CRLF escaping, compatible headers and columns, filename and media type, the optional trimmed search parameter, and bounded filtered and unfiltered queries.
- **Task 2.4:** Added a focused pagination policy to `GET /api/users` while preserving the existing endpoint, query parameters, response pagination fields, and `page=0`, `size=10`, `username asc` defaults. Page values must be non-negative integers, sizes must be integers from 1 through the documented maximum of 100, sort fields are restricted to the six fields used by the desktop client, and directions are limited to `asc` or `desc`. Invalid values return stable 400 responses with the existing message-body shape before any user query runs. The root README documents the bounds, defaults, and sort allow-list.
- **Task 2.4 verification:** `.\mvnw.cmd "-Dtest=UserPaginationControllerTest,UserControllerResponseTest" test` passed with 18 tests, and `.\mvnw.cmd test` passed with 48 tests, 0 failures, and 0 errors. Coverage includes compatible defaults and response metadata, the inclusive size cap, every client sort field, descending sorting, negative pages, zero/negative/oversized sizes, and rejected sort fields and directions.

- **Task 2.3:** Replaced direct JPA `User` deserialization and controller persistence loops with a restricted import DTO and transactional import service while preserving `POST /api/users/batch`, multipart field `file`, and the `totalRecords`, `successfulImports`, and `failedImports` success fields. Imports are limited to 1 MiB and 1–1,000 records; required fields and `ROLE_USER`/`ROLE_ADMIN` values are validated before persistence; unknown fields are rejected; password/hash, ID, status, timestamp, and role-ID input is ignored. Each accepted user receives a unique server-generated credential encoded by the configured password encoder and is imported disabled. In-file and existing-database username/email duplicates remain counted as failed imports.
- **Task 2.3 verification:** `.\mvnw.cmd -Dtest=*Import* test` passed with 10 tests, and `.\mvnw.cmd test` passed with 33 tests, 0 failures, and 0 errors. Coverage includes valid restricted input, privileged-field attempts, server credentials, disabled status, allow-listed roles, missing fields, unknown fields, malformed JSON, empty/oversized files, unsafe record counts, in-file/database duplicates, multipart compatibility, and result-count shape. The required direct-entity/password-no-op `rg` scan returned no matches.
- **Task 2.2:** Replaced the shared repository-root `users.json` write with an in-memory octet-stream download while preserving `GET /api/users/generate/{count}`, the `adminCount` query parameter, the `users.json` filename, and the existing generated JSON field names and types. Generation now uses an explicit response DTO with no password/hash, marks every generated account disabled, accepts total counts from 1 through 1,000 and admin counts from 0 through the total count, and returns a stable 400 message outside those bounds. The bounds are also documented in the root README.
- **Task 2.2 verification:** `.\mvnw.cmd test` passed with 23 tests, 0 failures, and 0 errors, including 8 generation tests covering compatible download headers and fields, role allocation, disabled/redacted output, inclusive bounds, stable invalid-count responses, and repository-file non-creation. `rg -n 'Files\.write|Paths\.get\("users\.json"' src\main\java` returned no matches.
- **Task 2.1:** Added explicit user, role, and paginated-list response DTOs plus a dedicated mapper. Profile reads, self/admin updates, paginated lists, and ID lookups now preserve the existing client-consumed JSON fields without returning JPA `User` or `Role` entities, password data, or the persistence-only role ID.
- **Task 2.1 verification:** `.\mvnw.cmd test` passed with 15 tests, 0 failures, and 0 errors, including 3 controller response tests for single-user fields, pagination fields, redaction, and DTO use across profile/update paths. The required controller scan found no `ResponseEntity.ok(user)`; its two remaining `getContent()` matches only build the existing internal CSV export list and do not return `User` entities as HTTP response bodies.
- **Task 1.1:** The Spring context test now activates the `test` profile and verifies that its JDBC connection uses the disposable PostgreSQL container rather than `localhost:5432`.

### Desktop client

- **Task 5.7:** Added a reproducible JavaFX desktop packaging workflow using the JDK's `jpackage --type app-image` support for the client's existing non-modular classpath. The workflow runs a clean client test build first, stops on failure, assembles the application JAR and pinned runtime dependencies, and produces a platform-native image named `TeamAccessHub` with the production launcher and bundled Java runtime. The packaged application retains the existing `TEAM_ACCESS_HUB_API_BASE_URL` configuration and local fallback. The JavaFX README documents the exact command, prerequisites, output and launcher paths, API URL configuration, platform-specific builds, lack of cross-compilation, and the unsigned app-image scope. Architecture, API contracts, and the database schema are unchanged.
- **Task 5.7 verification:** The documented alternative validation command `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\javafx-client\package.ps1` passed. Its clean pre-packaging test phase ran 13 JavaFX tests with 0 failures, 0 errors, and 0 skipped tests; `jpackage` then produced and verified `javafx-client/target/distribution/TeamAccessHub/TeamAccessHub.exe` plus its application files and bundled runtime. `PackagingConfigurationTest` passed 3 focused workflow-order, launcher, failure-checking, documentation, API-configuration, and platform-limitation assertions. A hidden-process smoke check started the packaged launcher with a non-default `TEAM_ACCESS_HUB_API_BASE_URL`, confirmed that it remained running after initialization, and stopped it cleanly. `git diff --check` passed.
- **Task 5.2:** Added an injectable `ApiClient` transport boundary for the JavaFX client, with explicit `OkHttpClient`, normalized base URL, and Gson dependencies. `ApiService` now composes that replaceable transport with an injected `SessionManager`; its protected operations obtain the authorization header from the in-memory session instead of requiring every controller call to pass token state through. The existing singleton accessors remain runtime conveniences, while public constructors allow isolated transport and session instances in tests. Dashboard workflows retain their existing API operations and user-visible behavior, and authentication state remains memory-only with logout clearing both the authentication response and current user.
- **Task 5.2 verification:** `.\mvnw.cmd -f javafx-client\pom.xml test` passed with 10 tests, 0 failures, and 0 errors. Tests inject the HTTP client, random-port MockWebServer base URL, Gson mapper, and independent session token without reflection or singleton replacement; they verify compatible authentication/profile routing, authorization-header use, silent sensitive operations, isolated in-memory session behavior, and logout cleanup. A client API/service scan found no console logging or token-persistence APIs, and `git diff --check` passed.
- **Task 5.1:** Replaced the JavaFX client's hard-coded service URL with a focused client configuration boundary. The complete API base URL can now be supplied through the `teamaccesshub.api.base-url` JVM property or `TEAM_ACCESS_HUB_API_BASE_URL` environment variable, with the JVM property taking precedence and `http://localhost:9090/api` retained as the development fallback. Values are trimmed, trailing slashes are removed, and non-HTTP(S), relative, credential-bearing, query-bearing, and fragment-bearing URLs are rejected. `ApiService` retains its application singleton while accepting configuration directly for tests, so MockWebServer now uses a random port without reflection or process-global configuration mutation. The JavaFX README documents both configuration sources, precedence, validation, and the local default.
- **Task 5.1 verification:** `.\mvnw.cmd -f javafx-client\pom.xml test` passed with 8 tests, 0 failures, and 0 errors. Coverage includes default behavior, environment and JVM-property resolution, precedence, normalization, invalid values, injected random-port MockWebServer routing, and the existing sensitive-console-output regressions. The required `rg` scan returned no hard-coded localhost `BASE_URL` service constant, and `git diff --check` passed.
- **Task 1.2:** Added a headless JUnit 5 client test harness with OkHttp MockWebServer. The smoke test starts an isolated loopback server, consumes a JSON response with OkHttp, and verifies the recorded request without requiring JavaFX initialization or a running backend.
- **Task 1.2 verification:** `.\mvnw.cmd -f javafx-client\pom.xml test` passed with 1 test, 0 failures, and 0 errors.
- **Task 1.4:** Removed authentication-response, token/header, and profile-response console output from `ApiService`. MockWebServer regression tests capture both standard output and standard error and verify successful authentication and profile retrieval remain silent.
- **Task 1.4 verification:** `.\mvnw.cmd -f javafx-client\pom.xml test` passed with 3 tests, 0 failures, and 0 errors. `rg -n "Auth response|Authorization:|Profile response" javafx-client\src\main\java` returned no matches.

### Database and infrastructure

- **Task 5.6:** Added a GitHub Actions continuous-integration workflow for every push and pull request. The read-only Ubuntu job checks out a clean repository, installs Temurin JDK 17, caches Maven dependencies from both module POMs, verifies Docker availability for PostgreSQL Testcontainers, and runs the repository PowerShell verifier with a 20-minute timeout and per-ref cancellation. The verifier now selects the Windows or Unix Maven wrapper and uses a platform-neutral JavaFX POM path. Weekly Dependabot updates are configured independently for the backend, JavaFX client, and GitHub Actions, so dependency maintenance does not block the base verification job. The README documents the hosted behavior; architecture, API contracts, and the database schema are unchanged.
- **Task 5.6 verification:** `ContinuousIntegrationConfigurationTest` passed 4 focused workflow, permission, cache, dependency-update, and cross-platform assertions; `scripts/verify.tests.ps1` passed 3 ordered/fail-fast scenarios. The required `.\scripts\verify.ps1` validation passed locally with 170 backend tests and 10 JavaFX tests, all with 0 failures, 0 errors, and 0 skipped tests, and `git diff --check` passed. GitHub Actions run [29448274557](https://github.com/baderbenlhachemi/spring-javafx-user-management/actions/runs/29448274557) passed on commit `140c59a7df97ec9e6a6bce60c031d4008ca90d8a`; checkout, JDK/cache setup, Docker verification, repository verification, and cleanup all completed successfully.
- **Task 5.5:** Added a root `scripts/verify.ps1` command that runs the backend test suite first and the headless JavaFX client suite second from a stable repository-root working directory. The verifier uses the checked-in Maven wrapper, stops before later modules after any failure, returns a non-zero exit code, restores the caller's working directory, and does not read or modify developer environment configuration. Repository guidance now documents its JDK, PowerShell, Docker/Testcontainers, dependency-resolution, and script-execution prerequisites, plus focused module commands and a process-scoped execution-policy fallback.
- **Task 5.5 verification:** `scripts/verify.tests.ps1` passed 3 dependency-free orchestration scenarios proving successful backend-to-client order, immediate stop after a backend failure, and non-zero propagation after a client failure. The required `.\scripts\verify.ps1` validation passed in a temporary `powershell.exe -ExecutionPolicy Bypass` process because the validation host's default policy blocks all local scripts; no persisted execution policy was changed. The verifier ran 166 backend tests and 10 JavaFX client tests with 0 failures, 0 errors, and 0 skipped tests in either module.
- **Task 5.4:** Added a reproducible Docker Compose development stack for the backend and PostgreSQL 16. The backend image uses separate Maven build and Temurin JRE runtime stages and runs as a dedicated non-root user. Compose waits for PostgreSQL health before starting the API, checks API readiness through the public redacted readiness probe, and persists database data in a named volume. Database credentials and JWT signing material remain externally supplied, local environment files and generated artifacts are excluded from the image context, and the README documents startup, verification, and teardown. Existing architecture, API contracts, and database schema are unchanged.
- **Task 5.4 verification:** `docker compose config`, `docker compose up --build -d`, `docker compose ps`, `Invoke-RestMethod http://localhost:9090/actuator/health/readiness`, and `docker compose down` all passed using disposable externally supplied validation values and a newly created named volume. Both services reported healthy, readiness returned `{"status":"UP"}`, PostgreSQL's `flyway_schema_history` recorded migration version 1, and the API process reported the non-root identity `uid=100(app)`. `.\mvnw.cmd -Dtest=ContainerConfigurationTest test` passed with 3 tests, 0 failures, and 0 errors; final `docker compose config --quiet` and `git diff --check` also passed.
- **Task 5.3:** Added Spring Boot Actuator with a health-only web exposure policy. Anonymous orchestration checks can access `/actuator/health/liveness` and `/actuator/health/readiness`, while aggregate health remains authenticated and environment, beans, configuration-properties, and metrics endpoints are not exposed. Health component names and details are always redacted. Liveness uses the application availability state, and readiness explicitly combines readiness state with the PostgreSQL `db` contributor. Existing domain API contracts, architecture, and the database schema are unchanged.
- **Task 5.3 verification:** PowerShell rejected the plan's unquoted `.\mvnw.cmd -Dtest=*Health*,*Security* test` selector before Maven started because of its comma; the equivalent quoted command `.\mvnw.cmd "-Dtest=*Health*,*Security*" test` passed with 20 tests, 0 failures, and 0 errors, including 8 full-context health-security cases against PostgreSQL Testcontainers. `.\mvnw.cmd test` also passed with 163 tests, 0 failures, and 0 errors. Coverage proves public redacted probes, protected aggregate health, absent sensitive actuator endpoints, explicit readiness-group database membership, and a live database health contributor.
- **Task 3.1:** Added Flyway-managed PostgreSQL schema version 1 for the existing `roles` and `users` model, including identity sequences, primary/unique/check/foreign-key constraints, and indexes for role and creation-time queries. Hibernate now uses `ddl-auto=validate` in runtime and test profiles and no longer creates, updates, or drops the schema. The development profile keeps automatic baselining disabled by default, and the root README documents the one-time, backup-first adoption procedure for a verified pre-Flyway development database.
- **Task 3.1 verification:** `.\mvnw.cmd -Dtest=FlywayMigrationTest test` passed with 1 test that migrated an empty PostgreSQL 16 schema, verified its tables, constraints, indexes, and single V1 history entry, then successfully booted the application a second time against the same schema. The required `.\mvnw.cmd test` passed with 89 tests, 0 failures, and 0 errors.
- **Task 1.7:** Explicitly ignored root and nested Maven `target/` directories while retaining the existing exceptions for intentional source directories named `target`.
- **Task 1.1:** Added Spring Boot Testcontainers service-connection support with PostgreSQL 16 Alpine, automatic container lifecycle management, and an isolated `create-drop` test schema. Testcontainers 1.21.4 is pinned for Docker Engine 29 compatibility.
- **Task 1.1 verification:** `docker info` passed against Docker Desktop 29.5.3; `.\mvnw.cmd test` passed with 1 test, 0 failures, and 0 errors. PostgreSQL and Ryuk containers were removed after the test JVM exited.
- **Task 1.5:** Added a placeholder-only `.env.example`, ignored local `.env` variants, and documented the required shell/IDE/deployment environment variables without adding automatic `.env` loading.
- **Task 1.6:** Added environment-backed `dev` profile settings for optional administrator initialization and removed runnable default administrator credentials from the README.
