spring.jpa.hibernate.ddl-auto=create-drop
# Intentionally public, test-only key material; production startup never uses this profile.
demo.jwtSecret=${TEST_JWT_SECRET:AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=}
demo.jwtExpirationMs=${TEST_JWT_EXPIRATION_MS:60000}
